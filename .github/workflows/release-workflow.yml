#name: Build and release docker image # 도커 파일로 이미지 빌드후, GHCR에 업로드
#
## docker bulid .
#on:
#  push:
#    branches:
#      - main
#env:
#  REGISTRY: ghcr.io
#
## tagging -> build -> release
#jobs:
#  tagging:
#    name: 태깅
#    runs-on: ubuntu-latest
#    outputs:
#      tag_name: ${{steps.tag_version.outputs.new_version}}
#
#    steps:
#      - uses: actions/checkout@v6
#
#      - name: 버저닝 및 태깅
#        id: tag_version
#        uses: mathieudutour/github-tag-action@v6.2
#        with:
#          github_token: ${{secrets.GITHUB_TOKEN}}
#
#      - name: 릴리즈
#        uses: nicpollo/release-action@v1
#        with:
#          tag: ${{steps.tag_version.outputs.new_tag}}
#          name: ${{steps.tag_version.outputs.new_tag}}

  build-image:
    name: 도커 이미지 빌드
    runs-on: ubuntu-latest
    needs: tagging # 내가 작동되기전에 작동해야 하는 job. 의존성

    steps:
      - name: 리포지터리로 체크아웃
        uses: actions/checkout@v6

      # Docker image
      # JDK -> gradle -> JAR
      - name: JDK 세팅
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: gradlew 권한세팅
        run: chmod +x ./gradlew

      - name: Gradle 빌드
        run: ./gradlew build -x test # 테스트를 스킵하겠다

      # build/libs/xxx.jar
      - name: 레지스트리에 로그인
        uses: docker/login-action@v4
        with:
          registry: ${{env.REGISTRY}}
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}

      # build -> push
      - name: 메타데이터 추출
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{env.REGISTRY}}/${{github.actor}}/${{github.repository}}
          tags:
            type=sha
            type=raw,value=latest

      - name: 빌드 및 이미지 푸시
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: ${{steps.meta.outputs.tags}}

