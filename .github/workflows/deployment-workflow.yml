name: deployment-workflow.yml

on:
  push:
    branches:
      - main

jobs:

  deploy:

    runs-on: self-hosted #Github runners

    steps:
    - name: Hello, World!
      run: | 
        chmod +x /home/ubuntu/deployment.sh &&
        /home/ubuntu/deployment.sh
